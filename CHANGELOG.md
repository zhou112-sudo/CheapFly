# TicketPlatform 变更记录

## 2026-04-15 搜索请求地址规范化（修复 Safari “expected pattern” 报错）

### 修改内容
- `searchFlights` 对 `VITE_API_BASE` 做规范化：自动补全 `http(s)://`，本地 `localhost` / `127.0.0.1` 默认走 `http`。
- 在发起 `fetch` 前用 `URL` 校验最终地址；若仍不合法，给出明确中文提示；并对 WebKit 抛出的 `did not match the expected pattern` 转为可读中文说明。

### 修改原因
- 当环境变量里只写了域名（无协议）或拼接出非法 URL 时，部分浏览器会在 `fetch` 阶段直接抛出该 DOMException；用户误以为是搜索逻辑问题，实际是接口基址配置问题。

## 2026-04-15 Vercel 部署：根目录无前端 package 导致构建失败

### 修改内容
- 在仓库根目录新增 `vercel.json`：
  - `installCommand` / `buildCommand` 在 `frontend` 目录执行 `npm ci` 与 `npm run build`
  - `outputDirectory` 指向 `frontend/dist`
  - 增加 SPA 路由回写，避免直接访问 `/explore`、`/deal/:id` 等子路径刷新 404
- 补充 `framework: null`，避免 Vercel 将仓库误判为 **Next.js** 并检查根目录 `next` 依赖（本项目为 **Vite + React**，`next` 不存在于 `frontend/package.json` 时即报错）。

### 修改原因
- 项目为「根目录 + `frontend/` 子目录」结构，Vercel 默认在仓库根执行安装与构建，找不到根级 `package.json` 或构建产物不在默认路径时会失败；显式配置后可稳定部署前端静态站点。
- 子目录前端在部分检测逻辑下会被误判为 Next 项目，需显式关闭框架预设，仅使用自定义 install/build + 静态输出目录。

## 2026-04-14 结果页新增往返模式（组合结构）

### 修改内容
- 在 `trip_type=roundtrip` 时启用往返结果分支：顶部摘要同时展示去程日期与返回日期。
- 相邻日期模块拆分为两组：`去程日期最低价` 与 `返程日期最低价`，并支持分别切换日期。
- 新增往返组合数据模型：按“去程航班 + 返程航班”生成组合，`总价 = 去程价 + 返程价`（同航司可小幅优惠）。
- 新增往返核心卡与列表：
  - `RoundTripRecommendedCard`：全网最低价（往返）+ 去返程信息 + 总价 + 平台标签 + 立即预订 + 详细信息
  - `RoundTripSummaryRow`：直飞最低（往返）/中转最低（往返）
  - `RoundTripListTable`：一行一个往返组合（去程+返程+总价+详细信息+立即预订）
- 往返场景的“详细信息”改为逐航段入口：去程和返程各自有独立“详细信息”入口，不再使用单个整体详情入口。
- 往返列表新增筛选排序：去程时段、返程时段、经停组合筛选；默认按总价从低到高。

### 修改原因
- 往返决策核心是“组合总价与去返程搭配”，不能再用单程列表表达；通过往返分组与双日期联动提升可读性与可比性。

## 2026-04-14 相邻日期价格联动调试增强（开发态）

### 修改内容
- 在结果页相邻日期价格拉取完成后，新增开发环境调试输出（`console.table`）：打印日期、当前舱位、对应最低价，便于快速确认“整条日期带是否按当前舱位重算”。

### 修改原因
- 当本地后端未重启或缓存状态干扰时，页面表现可能与预期不一致；开发态可直接核对每个日期价格来源，缩短排查时间。

## 2026-04-14 相邻日期价格去重与舱位联动修复

### 修改内容
- 后端价格计算新增“日期偏移调节”：在 `search` 返回时按 `departure_date + cabin_class + flight.id` 生成稳定偏移，叠加周末/工作日与近日期敏感度，让相邻日期价格稳定但有差异，不再大量重复同价。
- 该调节对 catalog 航班与合成航班统一生效，并随舱位（经济/超级经济/商务/头等）放大波动幅度，保证不同舱位整条日期带都会重算。
- 结果页修复 `routeState.data` 复用导致的舱位旧数据问题：仅当 URL 查询参数（起终点/日期/舱位）与 state 数据完全一致时才复用；否则强制按当前参数重新请求。
- URL 同步补齐 `cabin_class`，避免被旧同步逻辑覆盖回经济舱。

### 修改原因
- 解决“相邻日期价格一排相同”与“切舱位只更新当天、其他日期不更新”的可信度问题，保证日期条与当前查询上下文严格一致。

## 2026-04-14 低价卡文案与次级卡信息增强

### 修改内容
- 主卡标题由“全场最低价”调整为“全网最低价”。
- “直飞最低 / 中转最低”两张次级卡补充更完整的航班信息：新增航司+航班号、起降时间、中文路线行，并保留经停说明、详细信息入口与立即预订按钮。

### 修改原因
- 统一“最低价”命名语义并提升次级卡的信息密度，减少用户在主卡与列表之间反复切换查看详情。

## 2026-04-14 低价卡区域新增“详细信息”悬停入口

### 修改内容
- 在“全场最低价”主卡右侧新增“详细信息”文本入口（灰色小字、下划线），悬停显示航班详情浮层。
- 在“直飞最低 / 中转最低”两张次级卡右下区域同样新增“详细信息”入口，并与“立即预订”并排，支持悬停查看详情。
- 详情浮层复用现有 `FlightPreviewContent` 与 HoverCard 延迟开关逻辑，保持交互一致。

### 修改原因
- 让三张低价卡在不跳转的情况下都能快速查看航班详情，减少来回切换成本并统一操作习惯。

## 2026-04-14 全场最低价主卡右上区间距优化

### 修改内容
- 调整主卡右上角“平台标签 / 价格 / 立即预订”三者的纵向间距：标签与价格之间增加上间距，价格与按钮之间增加间距，避免元素贴得过近。

### 修改原因
- 提升主卡右侧信息层级与可读性，减少视觉拥挤感。

## 2026-04-14 全场最低价文案微调

### 修改内容
- “全场最低价”主卡右侧价格展示去掉“起”字，仅保留价格数字（如 `¥428`）。
- 移除主卡右侧价格下方重复的“全场最低价”小字；将紫色平台标签上移到该卡片右上角，信息层级更清晰。

### 修改原因
- 该卡片已明确表示“全场最低价”，继续显示“起”会造成语义重复。


## 2026-04-14 预订按钮与顶部 CTA 精简

### 修改内容
- 删除结果页摘要区右上角重复 CTA（“¥X 元起预订”），避免与主卡和列表内预订入口重复引导。
- 统一结果页关键按钮文案：将“预订”替换为“立即预订”，覆盖全场最低价主卡、直飞/中转次级卡、全部行程列表按钮。
- 统一主按钮配色为深灰系：`#2F2F2F` 背景、白字、hover `#222222`，禁用态为浅灰，替换原先视觉过重的纯黑感。

### 修改原因
- 减少重复 CTA 带来的决策噪音，强化主路径；同时统一按钮语义与灰度视觉体系，提升一致性与转化导向。

## 2026-04-14 相邻日期价格色彩阈值收紧

### 修改内容
- 调整 `NearbyDatesPriceBar` 的高低价着色判定：收紧“接近最高/最低”区间，避免价格接近时大面积误标红/标绿。
- 扩大“全黑显示”保护条件：当 7 天价差不明显时（按 `max(20, avg*6%)`）统一保持黑色，减少误导。
- 高价判定改为“明显高于均值”或“接近最高且高于均值”；低价判定同理改为更严格组合条件。

### 修改原因
- 解决“价格都差不多却几乎全红”的问题，使红绿仅用于明显高低价，普通波动默认黑色更符合用户预期。

## 2026-04-14 低价卡文案收敛与次级卡行动按钮

### 修改内容
- 主卡标题由“推荐方案”改为“全场最低价”，强调该卡即最低价核心信息。
- 主卡副文案改为用户导向表达：“当前查询结果中的最低价格，性价比最高，建议尽快预订。”，并将价格下方小字统一为“全场最低价”。
- “直飞最低 / 中转最低”两张次级卡新增右下角“预订”按钮（沿用现有黑底白字按钮体系，尺寸略小于主卡按钮），支持直接跳转对应航班预订链接。

### 修改原因
- 统一最低价模块命名与语气，减少“推荐/方案”主观表述；同时补齐次级卡片的直接行动入口，提升从比价到下单的连贯性。

## 2026-04-14 全部行程控制条：删除航司筛选并统一筛选排序样式

### 修改内容
- 删除“按航司筛选”整栏，仅保留 MVP 级筛选能力：行程类型、时段、价格与排序。
- 将“时段 / 价格 / 排序”从原生 `select` 改为统一风格的轻量菜单按钮（圆角、浅边框、黑白灰），与“仅直飞/经停”胶囊筛选同一视觉系统。
- 控制条布局重排为两行：第一行为行程类型胶囊，第二行为时段/价格/排序控制与重置入口；保留结果数量实时反馈。
- 保留原有筛选与排序逻辑能力与联动行为，切换后列表即时更新；保留“重置筛选”。

### 修改原因
- 减少占位和视觉噪音，避免“航司整栏”干扰主列表；同时让筛选与排序控件更产品化、更融入当前极简页面风格。

## 2026-04-14 价格来源标签：去除模糊词并强制平台实名

### 修改内容
- 前端价格来源标签规则改为“仅显示具体平台名或官网”：`FlightResults` 与 `RecommendedFlightCard` 的平台映射统一支持 `携程 / 飞猪 / 美团 / 同程 / 去哪儿 / Trip.com / 官网`。
- 完全移除模糊来源词在 UI 的输出路径：无来源或未知来源时强制回退为 `官网`，不再出现泛化文案。
- 标签数据读取优先使用 `platforms` 数组；若同价最低对应多平台，按数组并列展示多个紫色标签；若仅单平台则只显示该平台。
- 后端 mock 响应新增并标准化 `platforms` 字段：合成航班生成时写入 1~2 个平台；`_with_cabin_price` 对旧 `source` 做规范映射并兜底为 `官网`，确保前端始终拿到可展示平台。

### 修改原因
- 删除“合作渠道”等抽象词后，用户可直接识别价格来自哪个平台，提高信息可信度与决策效率。

## 2026-04-14 全部行程：对齐修复 + 详细信息文案 + 筛选排序

### 修改内容
- `FlightListTable` 统一标题区、筛选区、表格区横向内边距，修复“全部行程”标题与下方内容起点不齐的问题，提示文案也与内容同栅格对齐。
- 行内入口由“具体信息”改为“详细信息”，样式改为灰色小字+下划线文本链接；保留原 `HoverCard` 详情浮层与延迟开关逻辑。
- 新增轻量筛选区（列表标题下方）：支持经停筛选（全部/仅直飞/1次/2次及以上）、时段筛选（凌晨/上午/下午/晚上）、价格区间筛选（500以下/500-800/800以上）、航司多选筛选与“重置筛选”。
- 新增排序下拉：价格低到高（默认）、价格高到低、出发时间早到晚、出发时间晚到早、总时长短到长；与筛选联动并实时更新结果条数。
- 新增筛选后空状态提示，避免空表格带来的理解成本。

### 修改原因
- 让“全部行程”作为一个完整结果区在视觉上更整齐；并补齐用户高频的筛选与排序能力，降低浏览成本、提升决策效率。

## 2026-04-14 结果页信息结构优化：合并主卡与去重标签

### 修改内容
- 全局移除“多平台汇总”文案：平台来源统一按具体渠道展示（携程/飞猪/美团/官网/合作渠道）。
- 合并“最低价”和“推荐方案”：删除原“最低价”主卡，把最低价信息并入 `RecommendedFlightCard`，并强化为主视觉（大号价格 + “当前全场最低价”文案 + 预订按钮）。
- 推荐方案主卡新增平台紫色标签逻辑：若同价最低来自多个平台则并列展示多个标签；否则仅展示当前推荐航班所属平台。
- “直飞最低 / 中转最低”保留为次级对比卡，弱化视觉权重。
- 价格参考卡删除右上角“便宜/适中/偏贵”标签，仅保留数值、趋势图和说明。
- 结果页模块顺序调整为：推荐方案（主）→ 直飞/中转最低（次）→ 全部行程 → 价格参考（辅助）。

### 修改原因
- 减少重复与模糊信息，避免同一层级出现两个“最低价”入口；让用户第一眼看到可行动的最低价信息，并保持辅助模块简洁。

## 2026-04-14 结果页价格区：主最低价卡与平台标签

### 修改内容
- `FlightSummaryRow` 改为「主最低价卡 + 次级价格卡」：顶部主卡突出“最低价”与大号价格，次级仅保留“直飞最低 / 中转最低”，视觉权重下沉。
- 主最低价卡新增平台来源紫色标签（低饱和）：若同价来自多个平台则并列展示多个 Tag（如携程 / 飞猪 / 官网）。
- 主卡新增决策文案（如“当前为全场最低价，建议尽快预订”），强化第一屏价格决策。
- 删除结果页“实惠中转”模块（`CheapConnectionsSection`）的渲染入口，避免与“中转最低”信息重复。

### 修改原因
- 降低多价格并列造成的决策负担，让用户第一眼看到最便宜价格并快速行动；同时保留直飞/中转对比信息但不抢主视觉。

## 2026-04-14 全部行程：路线中文化与“具体信息”悬停预览

### 修改内容
- 列表中的航线信息统一改为中文机场名展示：使用 `localizeRouteLabel(route_label)`（中文机场名 + 三字码）；补充常见国际机场中文兜底映射（如 HKG/NRT/ICN 等），避免仅显示代码。
- 在每条行程右侧新增轻量入口「具体信息」：悬停显示 `HoverCard` 浮层，内容复用 `FlightPreviewContent`，包含航司/航班号、出发到达时间、总时长、出发/到达机场、经停与票务规则等；带 open/close delay 降低闪烁。
- `FlightPreviewContent` 内的航线与机场字段同步中文化：航线改为 `localizeRouteLabel`，机场行优先用 `airportDisplay`，经停点优先按航线中转码显示中文机场名。

### 修改原因
- 与中文界面保持一致，降低阅读成本；同时提供不打断列表的轻量详情预览，提高比较效率。

## 2026-04-14 搜索结果页：相邻日期最低价参考

### 修改内容
- 搜索结果页顶部新增 `NearbyDatesPriceBar`（日期价格条）：以当前出发日为中心展示前后 3 天共 7 天（不会早于本地今天），每格显示「日期 + 该日可订最低价」。
- 日期条价格与结果页保持一致：复用 `applyDepartureWindowToSearchPayload` 同口径过滤，并新增 `lowestFilteredPriceCny` 用于提取该日期最低可用价。
- 点击日期卡片会触发该日期重新搜索：同步更新 URL `departure_date`、结果页摘要日期、推荐方案、价格参考、汇总与航班列表。
- 新增轻量提示文案：若近 7 天存在更低价会提示可改期；否则提示当前日期为可见低价。
- `dates.js` 新增日期工具：`addCalendarDays`、`formatDepartureShortZh`、`buildNearbyDepartureDates`，用于生成稳定且可控的相邻日期窗口。
- 日期条价格新增高低价色彩标识：基于 7 天 `min/max/avg` 计算低价（低饱和绿）/高价（低饱和红）/正常（黑）；若价格差过小则全部保持黑色，避免误导。

### 修改原因
- 在不改变现有整体布局与风格的前提下，补齐主流机票产品常见的「相邻日期比价」决策能力，帮助用户更快发现更便宜出行日。

## 2026-04-14 最低价走势图：标题强调、纵轴字号与按航线稳定 mock

### 修改内容
- 图表标题改为「**最低价**走势预测」：`最低价` 加粗，整体字号略大于原 `text-xs`；仍与全站克制风格一致。
- 纵轴 ¥ 刻度改为 **8px**、颜色略浅于横轴，横轴时间仍为 10px，避免纵轴抢占视觉。
- 示意 `trend` / `forecast`：**按 `origin_airports`、`destination_airports`、`departure_date` 做 FNV-1a seed + 与前端一致的步进算法**，同条件结果稳定、不同航线形态与波动幅度不同；结合目录/机场库判断国内与国际、热门线、短途等调整价差与起伏（非真实行情）。
- 前端 `flightTimeRules.buildRouteTrendForecast` + `buildInsight(..., query)`；后端 `main._build_route_trend_forecast` + `_build_insight` 传查询串，直链刷新与客户端时间过滤后逻辑对齐。

### 修改原因
- 纵轴过大会显得「像 demo」；标题需一眼识别主题。
- 原固定加减点数导致任意搜索曲线雷同，降低可信度；在仍为示意数据的前提下用确定性 seed 体现航线差异。

## 2026-04-14 价格参考：Y 轴、最低价聚焦与去掉历史高位

### 修改内容
- 价格示意折线图增加左侧 **¥ 价格刻度**（约 4 档、浅灰细线），与黑白灰风格一致。
- 图表标题改为「最低价走势与预测」；曲线数据改为「可订最低价」回落到今天再小幅波动的示意；**移除**「历史高位」卡片及接口字段 `historical_peak_cny`。
- 「今天」不在图内单独贴字，仅在横轴标签行展示；**横轴时间与纵轴价格刻度同一字号**，辅助说明更小、更浅。
- **最低价走势图**：纵轴改为 **固定步长等间距刻度**（约 4～5 档，按范围自动取整），映射与刻度一致；主线深灰、预测浅灰虚线、**无渐变**；仅横向极浅网格；「今天」节点为小圆点、无光圈式描边。
- 便宜/适中/偏贵：改为仅用 **历史低价 + 近期均价** 推导区间位置（不再使用历史最高价）。

### 修改原因
- 提升读数能力并降低噪音；高位信息对决策帮助有限，聚焦最低价更符合当前产品表达。

## 2026-04-14 价格参考：便宜/适中/偏贵判定逻辑

### 修改内容
- 以「当前最低价」在历史低价～高价区间内的归一化位置 `position`，并结合是否明显低于/高于「近期均价」综合判定 `recommendation_level`（便宜 / 适中 / 偏贵）；同步更新配套推荐文案。
- 前端 `flightTimeRules.buildInsight` 与后端 `_build_insight` 保持一致。

### 修改原因
- 原先用「均价与当前价差」档位划分，易出现「实际已偏低仍显示适中」；改为基于历史区间位置 + 与均价关系更符合直觉。

## 2026-04-14 价格走势示意：黑白灰与横轴文案

### 修改内容
- `PriceInsightCharts`：曲线与节点改为低饱和黑灰系，预测段浅灰虚线；分区底与竖线弱化；「今天」轻量标注；标题区「示意」弱提示；底部说明精简。
- `flightTimeRules` / 后端 `price_insight`：趋势横轴由「M-n / 本周」改为「5周前 … 1周前 / 今天」，预测为「1周后 … 4周后」。
- `PriceInsightCard`：卡片副标题略短。

### 修改原因
- 原蓝色高对比与 M-n 横轴与全站黑白灰气质不一致；相对周次中文更贴近用户阅读。

## 2026-04-14 出发日期校验与结果页可预订时间过滤

### 修改内容
- 搜索页提交前：若去程或返程（往返）日期早于**本地日历**「今天」，拦截搜索并提示「不能查询过去日期的航班。」，不进入结果页。
- 结果页（含直链加载）：对 `/search` 返回的航班按**本地当前时间**过滤，仅保留「出发时刻 ≥ 现在 + 1 小时」的行程；并重算最低价、价格参考、推荐与中转推荐等派生字段。
- 默认去程日、日期比较统一为**本地** `YYYY-MM-DD`（`localCalendarYmd`）；无可展示航班时在结果摘要与列表区展示「当前时间范围内暂无可预订航班」类说明，不报错误。

### 修改原因
- 避免查询已过去日期；当日查询需隐藏已起飞或过近无法成行的班次，贴近真实预订体验。

## 2026-04-14 结果页「价格参考」图表与状态标签

### 修改内容
- `PriceInsightCharts`：将「历史走势」与「未来预测」合并为**一张**横轴连续图；历史段实线、预测段虚线，竖虚线标注「当前」节点，左右轻渐变区分过去/未来；平滑路径 + 当前节点外圈强调；副标题弱化「示意」为文案区说明。
- `PriceInsightCard`：状态标签配色——便宜（绿）、适中（橙）、偏贵（红），浅底与细边框；文案与等级对齐为「便宜 / 适中 / 偏贵」。

### 修改原因
- 双图并列信息割裂，单图更符合「历史—当前—预测」连续阅读；状态色与语义一致可降低理解成本。

## 2026-04-14 地点下拉：省份/国家搜索与无结果文案

### 修改内容
- `airportCatalog` 升至 v2：为中国城市写入 `province`（省份，用于检索）；为非中国城市写入 `countryEn`（国家英文别名，便于匹配如 Japan / USA）；新增琼海博鳌（`BAR`）等以覆盖「海南」等省份检索示例。
- `locations.js` 的 `buildCitySearchBlob` 纳入 `province`、`countryEn`，省份/国家检索仍只展示既有「城市 → 机场」结构。
- 地点下拉无匹配时改为纯文字「无结果」，去掉前缀符号。

### 修改原因
- 用户常按省份或国家名缩小范围，仅靠城市/机场字段易误判为无结果；无结果区需更克制、与整体轻量风格一致。

## 2026-04-14 结果页路线标题与城市选择一致

### 修改内容
- `ResultsPage` 顶部「出发 → 到达」在存在路由 `state.query` 中的地点项时，改为使用 `locationPillLabel`：依据 `kind === "city"` / `"airport"` 展示「城市（所有机场）」或单机场全称，不再对 `codes` 调用 `summarizeAirportGroup`（避免单机场城市被显示成具体机场名）。

### 修改原因
- 用户选择「城市（所有机场）」时期望结果页与搜索页文案一致；原先仅用代码列表推断，会把仅含一个三字码的目的地显示成该机场节点。

## 2026-04-14 出发/到达本地机场目录与基础搜索

### 修改内容
- 新增前端本地机场目录 `frontend/src/data/airportCatalog.json`（当前约 81 个城市分组、100 个机场；中国主要城市 + 国际主要枢纽为主，可持续扩充）。
- 新增生成脚本 `frontend/scripts/build-airport-catalog.mjs`，并在 `frontend/package.json` 增加 `npm run gen:airports`。
- `frontend/src/data/locations.js` 改为从目录 JSON 构建 `CITIES`、地点选项与预计算搜索串；`filterPickerCities` 支持多 token 子串匹配（城市/机场中英文字段、IATA、`keywords`）。
- 地点选择器无匹配时展示「无结果」；`airportDisplay` 在有 `pickerName` 时与选择列表展示一致。

### 修改原因
- 原手写少量城市无法满足「多字段 + 更广覆盖」的搜索体验；本地 JSON + 前端过滤实现简单、响应快，且与后端 mock/动态航班逻辑解耦。

## 2026-04-13 搜索条件返回保留优化

### 修改内容
- 在 `SearchPage` 新增 URL query 持久化逻辑，保存并恢复：
  - 出发地 `originId`
  - 到达地 `destinationId`
  - 日期 `date`
- 页面初始化时直接从 URL 读取并作为初始 state，确保返回搜索页后输入框立即显示上次选择。
- 增加搜索页 state -> URL 的同步（`replace` 模式），避免历史记录膨胀。
- 保留原有搜索流程与 UI，不改变搜索结果请求参数结构。

### 修改原因
- 用户从结果页返回搜索页时，希望保留已填写的出发地/到达地/日期，便于继续调整与二次搜索，减少重复输入。

## 2026-04-13 搜索条件保留修正（query + sessionStorage 兜底）

### 修改内容
- 修正搜索页状态恢复逻辑：在 URL query 基础上增加 `sessionStorage` 兜底恢复。
- 当用户通过“首页/返回”等不携带 query 的路径回到搜索页时，仍可恢复最近一次填写的：
  - 出发地
  - 到达地
  - 日期
- 保持 state -> query 的同步逻辑不变，仍使用 `replace` 写入 URL。

### 修改原因
- 仅依赖 query 时，某些返回路径会丢失参数，导致搜索页显示为空；增加本地会话兜底可提升恢复稳定性。

## 2026-04-13 往返查询（第一阶段：搜索页）

### 修改内容
- 在搜索页新增“单程 / 往返”切换（默认单程）。
- 切换到“往返”后，输入结构扩展为：
  - 出发
  - 到达
  - 去程日期
  - 返程日期
- 统一单程/往返布局：单程下也保留返程栏位（显示“单程无需返程”），避免界面跳变。
- 新增“机票种类”选择：
  - 经济舱
  - 超级经济舱
  - 公务舱
  - 头等舱
- 调整“机票种类”区域布局：
  - 去掉右侧提示卡片
  - 机票种类改为左下角小尺寸展示
  - 下拉控件视觉贴近地址选择器（圆角边框 + 右侧箭头）
- 按照参考样式进一步简化“机票种类”：
  - 去掉边框与标题
  - 改为纯文本样式下拉（右侧小箭头）
  - 初始默认值维持为“经济舱”
- 调整机票种类控件尺寸与位置：
  - 字号缩小为次级文本级别（小于出发/到达主内容）
  - 行高与箭头同步缩小
  - 保持左下角展示
- 增加往返日期校验：
  - 往返模式下必须填写返程日期
  - 去程日期必须早于返程日期
- 搜索页状态持久化参数补充：
  - `tripType`（`oneway` / `roundtrip`）
  - `returnDate`（`YYYY-MM-DD`，仅往返）
  - `cabinClass`（`economy` / `premium_economy` / `business` / `first`）

### 修改原因
- 为后续结果页往返组合展示做输入层准备，先完成低风险、可控范围内的搜索页能力扩展。

## 2026-04-13 后端舱位数据与查询打通

### 修改内容
- 在 `mock_flights.json` 的每条航班中新增 `cabin_prices`，提供四档舱位价格：
  - `economy`
  - `premium_economy`
  - `business`
  - `first`
- 后端 `/search` 新增查询参数 `cabin_class`，并按舱位返回对应价格与 `cabin_class` 字段。
- 前端 `searchFlights` 正式透传 `cabin_class` 到后端，搜索结果价格随舱位变化。

### 修改原因
- 之前舱位选择仅停留在前端状态，未影响真实查询结果；本次将“舱位”接入 mock 数据与后端筛选，形成闭环。

## 2026-04-13 数据库扩容（机场覆盖 + 动态航班）

### 修改内容
- 后端接入 `airportsdata` 数据源（IATA 维度），作为机场基础库：
  - 覆盖中国机场全量条目（按数据源）
  - 覆盖国外主要机场（按数据源）
- 当静态 `mock_flights.json` 无匹配结果时，后端会按“出发/到达/日期”动态生成示例航班（含直飞和中转）。
- 动态生成航班同样包含 `cabin_prices`，并继续支持 `cabin_class` 查询。

### 修改原因
- 仅靠少量静态 mock 航班会导致大量查询无结果；引入机场基础库 + 动态生成可显著提升可查范围与演示稳定性。

## 2026-04-14 搜索区标签外置与机场全称展示

### 修改内容
- 搜索页主输入区：将「出发 / 到达 / 去程日期 / 返程日期」标签移到输入框外（桌面两行网格；小屏成对展示）。
- 调整桌面端列宽比例：地点列更宽、日期列更窄。
- 地点触发器展示：机场选择显示全称（与列表一致，如「北京首都国际机场（PEK）」）；城市全部机场显示为「城市名（所有机场）」。

### 修改原因
- 提升信息层级与可读性，并让长机场名称在触发器上完整呈现。

## 2026-04-14 城市级展示文案统一

### 修改内容
- 城市「全部机场」展示统一为「城市名（所有机场）」（触发器与结果摘要一致）。

### 修改原因
- 与产品文案口径一致，避免「全部机场」与「所有机场」混用。

## 2026-04-14 往返列表改为左右选择模式

### 修改内容
- 将往返结果列表从「一行一组预组合」改为「左右双栏自由组合」：
  - 左栏：去程航班列表
  - 右栏：返程航班列表
- 两侧均支持独立筛选与排序（互不影响）：
  - 时段筛选
  - 经停筛选（全部 / 仅直飞 / 仅中转）
  - 排序（价格低到高、价格高到低、出发时间早到晚、出发时间晚到早）
- 每条航班卡支持点击选中，并通过边框/背景高亮体现当前选择状态。
- 新增底部固定汇总区：
  - 展示已选去程与返程
  - 实时计算并显示总价（同航司默认给出小幅组合优惠）
  - 提供「立即预订」按钮
- 删除原组合列表中的「经停组合标签」展示，不再以系统预组合为主。
- `ResultsPage` 往返列表数据改为直接向组件传入去程与返程航班数组。

### 修改原因
- 用户需要像主流订票平台一样自由搭配去程与返程，而不是只能选择系统预先组合结果；通过左右独立选择可显著降低决策成本并提升可控性。

## 2026-04-14 往返筛选UI与单程统一

### 修改内容
- 往返左右栏筛选区改为与单程一致的控件风格：
  - 经停筛选改为顶部胶囊按钮（全部 / 仅直飞 / 仅中转）
  - 时段、价格、排序改为轻量 `MenuButton` 下拉菜单（替换原生 `select`）
- 左右栏新增独立价格筛选（全部 / 500以下 / 500-800 / 800以上），并保持互不影响。
- 保留左右独立重置逻辑，同时重置时段/经停/价格/排序到默认值。

### 修改原因
- 原往返筛选控件视觉与单程不一致，且原生下拉有“默认表单感”；统一后可提升整体产品一致性与可读性。

## 2026-04-14 “目的地精选”升级为“低价精选”

### 修改内容
- 导航文案统一从「目的地精选」改为「低价精选」。
- 精选页顶部重构为更清晰的 Hero 结构：
  - 主标题：`发现低价出行机会`
  - 副标题：强调“聚合低价航线、快速发现值得出发的机票”
- 主内容区新增分区标题：
  - `今日低价推荐`
  - `优先展示近期价格更低、性价比更高的热门航线`
- 精选卡片从“目的地名 + 参考价”升级为“低价航线推荐卡”：
  - 目的地图片
  - 城市名
  - 出发地→目的地航线文案
  - 当前最低价
  - 折扣/低价标签（如 `5.6折`、`近期低价`）
  - 轻量价值说明（如“低于近30天常见价格区间”）
- 使用现有 mock 数据扩展字段（图片、路线、标签、说明），未改动搜索页逻辑与结果页逻辑。

### 修改原因
- 页面定位从“看目的地”升级为“发现低价机票机会”，让用户更快理解“为什么值得看”，提升决策效率与产品感知价值。

## 2026-04-14 低价精选 Hero 视觉对齐搜索页

### 修改内容
- 调整 `低价精选` 页顶部 Hero 布局，使其更接近搜索页首屏的视觉结构：
  - 更突出的居中大标题与副标题
  - 下方增加大尺寸白底圆角容器（带轻边框与阴影）
  - 主操作按钮保持居中，强调单一主行动线
- 保持原有浅色、简洁、卡片化风格，不引入复杂动效或高饱和配色。

### 修改原因
- 让 `低价精选` 与首页搜索区在视觉语言上更统一，减少页面切换时的风格跳跃，提升整体产品一致性。

## 2026-04-14 低价卡片按城市动态加载真实图片

### 修改内容
- 低价精选卡片图片源改为 `source.unsplash.com` 动态拉取，不再使用固定本地/静态图片链接。
- 新增城市关键词映射（中文城市名 -> 英文搜索词），按目的地自动生成图片 URL：
  - 例如：`上海 -> shanghai skyline`、`三亚 -> sanya beach`
- 新增 `getCityImage(city, index)`：
  - 根据城市与卡片序号生成 `800x600` 图片地址
  - 通过 `sig` 参数降低同屏重复图概率
- 增加加载失败兜底逻辑：
  - 首次失败回退 `/default-city.jpg`
  - 若本地兜底仍失败，再回退到通用城市远程图，避免出现破图图标
- 保持 16:9 比例与暗色渐变遮罩，维持文字可读性和原有卡片风格。

### 修改原因
- 让每张卡片更贴近真实目的地场景，强化“低价机票发现”页面的真实感与可浏览性，减少 Demo 感。

## 2026-04-14 低价卡片改为随机风景图

### 修改内容
- 按需求取消“按城市关键词映射图片”逻辑。
- 卡片图片统一改为随机风景图来源（`source.unsplash.com`），通过 `sig` 参数避免同屏重复。
- 保留原有图片失败兜底与视觉样式（16:9、圆角、渐变遮罩）。

### 修改原因
- 用户希望页面以统一风景氛围为主，不再强调城市强匹配图片，以获得更自然的浏览体验。

## 2026-04-14 低价精选卡片新增详情落地页

### 修改内容
- 低价精选卡片交互升级为整卡可点击，点击后进入对应推荐票详情页（`/deal/:id`）。
- 新增推荐票详情页 `DealDetailPage`，展示单张推荐票的完整信息：
  - 基础信息：出发地、到达地、出发日期、舱位、当前最低价
  - 推荐票详情：航司、航班号、时刻、时长、经停、机场、规则信息
  - 价格信息：当前价、低价标签、推荐说明、近30天均价对比文案、平台来源
  - 主按钮：`立即预订`
- 为每张低价卡片补充独立 mock 航班数据，确保不同卡片对应不同详情内容，而非同一静态页面。
- 保持低价精选页面现有卡片视觉样式，仅增强点击与跳转能力。

### 修改原因
- 解决“卡片只展示不落地”的问题，让用户从低价推荐可以直达单票详情并完成预订决策，提升转化路径完整性。

## 2026-04-14 随机图片多样性优化

### 修改内容
- 调整低价卡片随机图片生成策略，新增多主题关键词池（城市夜景、海滩、山川、湖景、地标、街景等）。
- 生成 URL 时按卡片索引和时间种子组合不同主题与 `sig` 参数，减少同屏重复“机窗图”问题。
- 保留原有失败兜底逻辑与卡片视觉样式不变。

### 修改原因
- 之前随机关键词过于集中，导致同屏图片同质化明显；本次提升图片内容多样性，增强浏览体验与真实感。

## 2026-04-14 随机图来源改为固定多图池轮换

### 修改内容
- 将低价卡片图片来源从 `source.unsplash.com` 即时随机改为“高质量风景图 URL 池”轮换策略。
- 按卡片索引与时间种子从图池中取图，保证同屏卡片明显不重复，并可按时间窗口自动换一批。
- 保留加载失败兜底逻辑，避免破图。

### 修改原因
- 即时随机接口在部分环境下会返回同源缓存图，导致“看起来没变化”；改为图池轮换后可稳定保证多样性。
